* Change IP_ADDRESS in deploy/inventory/* files
* Create secret files with field ENV=$(UR ENV)
* Add the key to the server to secrets/$ENV/server.pem
* \* Change the ansible_user variable in deploy/inventory/* files

\* - Optional
